package portfolio.model;

/**
 * This is an enum supporting the BuyType of every buy transaction. It basically mentions whether
 * the stocks are being bought on the basis of some amount provided or on the basis of the count.
 */
public enum BuyType {
  COUNT, AMOUNT
}
